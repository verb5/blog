name: CI - Integrate
on:
  push:
    branches:
      - 'main'
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
        name:  build test
      - run: echo "Integrating..."
      - run: echo "time is $(date)"
        shell: bash
  second-build:
    runs-on: self-hosted
    steps:
      - name:  second build test
        run: echo "time is $(date)"
        shell: bash
